"""
Optional standalone entry. Phase 2 already runs inside app.py when a
candidate joins LiveKit — you do not need this process for local interviews.
"""

print("Phase 2 AI interviewer starts from app.py on POST /api/livekit-token.")
print("Keep python app.py running; do not start a second copy on port 8000.")
